class PagesDataStore(url: String) {

    fun wasCrawled(): Boolean{
        // check if the page was crawled before
        // use simhash to check similarity BY SIGNATURE
        return false
    }

    fun putUrl(){

    }
}